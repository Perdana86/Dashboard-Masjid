{
  "name": "monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "setup": "npm install",
    "dev": "concurrently --kill-others --raw \"npm run dev --prefix apps/web\" \"npm run dev --prefix apps/api\" \"npm run dev --prefix apps/pocketbase\"",
    "build": "pwsh -ExecutionPolicy Bypass -NoProfile -File ./scripts/build.ps1",
    "start": "concurrently --kill-others --raw \"npm run start --prefix apps/web\" \"npm run start --prefix apps/api\" \"npm run start --prefix apps/pocketbase\"",
    "start:web": "npm run start --prefix apps/web",
    "start:api": "npm run start --prefix apps/api",
    "start:pocketbase": "npm run start --prefix apps/pocketbase",
    "lint": "concurrently --raw \"npm run lint --prefix apps/web\" \"npm run lint --prefix apps/api\""
  },
  "devDependencies": {
    "concurrently": "^10.0.4",
    "knip": "^6.32.1"
  },
  "workspaces": [
    "apps/*"
  ]
}
